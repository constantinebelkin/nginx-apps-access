# NGINX hosts access checker
Scripts for checking NGINX hosts for access.

* One time - check all of the hosts in one time
* Default - script optimized for Zabbix (passes each host per execution)

by amazingcat LLC
